<%- include('../partials/head')%>
<body>
<%- include('../partials/header')%>
    <div class="content-wrapper">
<main>
    <div class="post-maker-container">
    <h2>Create a new post</h2>
    <form action="/newpost" method="POST">
        <label>Add Text:</label><br>
        <input required type="text" id="message" name="message"><br>
        <!-- <label>Add Image:</label><br>
        <input type="file" name="myImage" accept="image/*"><br> -->
        <input type="submit" class="btn" value="Post">
    </form>
    </div>
    <!-- End of post maker -->
    <br>
    <hr>
    <h2>Recent Posts</h2>
    <div class="post-timeline-container">
        <!-- class allowing posts to display in a user friendly manner -->
    <ul>
        <% postData.forEach(post=>{ %>
            <!-- ^ Likely needs adjustment to not display all posts -->
            <li>
                <% if(post.imagePath){ %>
                        <img src="<%= post.imagePath %>" alt="" class="post-user-avatar">
                    <% } else { %>
                        <p>sorry no image available</p>
                    <% } %>
                <p>
                    <%= post.postedBy %>
                </p>
                <p>
                    <%= post.message %>
                </p>
                <button onclick="location.href='/like?postid=<%= post._id %>'">&#x2665 <%= post.likes%></button>
                <!-- '&#x2665' is a heart icon -->
                <a href="/viewpost?postid=<%= post._id %>">Comment</a>
            </li>
        <% }) %>
    </ul>
    </div>
    <ul class="timeline-nav-container">
        <li id="timeline-back-button"><a href="#">&laquo;</a></li>
        <li><a href="#">1</a></li>
        <li><a href="#">2</a></li>
        <li><a href="#">3</a></li>
        <li><a href="#">4</a></li>
        <li><a href="#">5</a></li>
        <li id="timeline-forward-button"><a href="#">&raquo;</a></li>
        <!-- navigation to allow user to look through further posts -->
         <!-- see if btn class stylings can be used -->
    </ul>
    <!-- Based on styling from: https://www.w3schools.com/css/css3_pagination_styles.asp -->
</div>
</main>
</body>
<%- include('../partials/footer')%>
